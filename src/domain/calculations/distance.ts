export const haversineNm=(a:{latitude:number;longitude:number},b:{latitude:number;longitude:number})=>{const r=3440.065,toRad=(v:number)=>v*Math.PI/180; const dLat=toRad(b.latitude-a.latitude),dLon=toRad(b.longitude-a.longitude); const x=Math.sin(dLat/2)**2+Math.cos(toRad(a.latitude))*Math.cos(toRad(b.latitude))*Math.sin(dLon/2)**2; return 2*r*Math.asin(Math.sqrt(x));}; export const travelMinutes=(nm:number,knots:number)=>knots>0?nm/knots*60:Infinity;